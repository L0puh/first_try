# compile with `cmake`:
```sh
mkdir build 
cd build  
cmake .. 
cmake --build . 
```

# todo:
### networking:
- [x] network: fix quiting the client (server crashes)
- [x] network: fix sending data  
- [x] network-chat: usernames 
